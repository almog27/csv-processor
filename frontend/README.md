React UI for csv precessor

Io order to use
1. Make sure you have the backend running (make run)
2. Install dependencies (make ui-prep)
3. Run the UI (make ui-run)
4. Open browser http://localhost:5173/